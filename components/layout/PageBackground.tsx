// Decorative page background. No CFAC imagery defined yet, so this renders
// nothing. Add a route→image map here later if CFAC provides brand imagery.
export default function PageBackground() {
  return null
}
